# ClaudeNT

## Shared brain — read first

Cross-surface context lives in `nareshtanna-bot/tanna-terminal` at
`memory/STATE.md` (local clones: Mac `~/AI/tanna-terminal`, PC
`C:\dev\tanna-terminal`; cloud sessions: fetch via gh —
`gh api repos/nareshtanna-bot/tanna-terminal/contents/memory/STATE.md --jq .content | base64 -d`).

Read it at session start. At session end, append a digest of what was
discussed/decided/changed to `memory/journal/YYYY-MM-DD.md` in that repo
(locally: edit + push; cloud: `claude/*` branch PR there — it auto-merges).
Full protocol: tanna-terminal `CLAUDE.md`.
